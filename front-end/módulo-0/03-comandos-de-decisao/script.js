// Método para encontrar o maior valor

function maiorValor(a, b) {
  if (a > b) {
    console.log(`O valor de a (${a}) é maior do que o valor de b (${b}).`);
  } else {
    console.log(`O valor de b (${b}) é maior do que o valor de a (${a}).`);
  }
}

maiorValor(5, 2);
maiorValor(2, 10);
